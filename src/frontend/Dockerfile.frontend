# Dev-only Dockerfile for running the Next.js frontend with hot reload
FROM node:22-slim

WORKDIR /app

COPY package.json ./

RUN npm install -g pnpm && pnpm install

COPY . .

ENV WATCHPACK_POLLING=true
ENV CHOKIDAR_USEPOLLING=true

EXPOSE 3000

CMD ["pnpm", "dev", "--hostname", "0.0.0.0"]